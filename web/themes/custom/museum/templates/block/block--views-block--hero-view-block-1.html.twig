{%
set classes = [
'block',
bundle ? 'block--' ~ bundle|clean_class,
id ? 'block--' ~ id|clean_class,
]
%}
<div{{ attributes.addClass(classes)|without('id') }}>
<div class="loading"><img src="/sites/default/files/2018-03/wide-hero-4.jpg"></div>
  <div class="block__content">
    {% block content %}
      {{ content }}
    {% endblock %}
  </div>
</div>
